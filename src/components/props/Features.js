import React, { Component } from "react";

class Features extends Component{
    render(){
        return <p>We will be displaying the height: {this.props.height} and id: {this.props.id}</p>
    }
}

export default Features 