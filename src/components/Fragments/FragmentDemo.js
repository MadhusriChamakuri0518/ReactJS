import React from 'react'

function FragmentDemo() {
  return (
    <React.Fragment>
        <h1>Fragment Demo</h1>
        <p>Instead of creating a extra node to the react dom, we use this fragments to keep all the returning content in the single return statement</p>
    </React.Fragment>
  )
}

export default FragmentDemo